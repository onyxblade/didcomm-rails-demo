{
  "name": "didcomm-http",
  "version": "1.0.0",
  "description": "Stateless HTTP wrapper for DIDComm v2 (didcomm-rust WASM bindings)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "didcomm-node": "^0.4.1"
  }
}
